# Heartbeat checklist

The gateway runs periodic heartbeat turns using this file as context (see `OPENCLAW_HEARTBEAT_*` in `.env`).

- [ ] Nothing urgent — reply `HEARTBEAT_OK` if the inbox is quiet.
- [ ] Review any open tasks from the operator in `TOOLS.md`.

Keep this short; the agent should not repeat old chat tasks.
